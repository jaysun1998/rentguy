{
  "name": "rentguy",
  "version": "1.0.0",
  "description": "RentGuy property management platform",
  "scripts": {
    "install": "cd backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && python run.py"
  },
  "engines": {
    "node": ">=18.x",
    "npm": ">=9.x"
  }
}
